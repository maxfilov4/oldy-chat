# Ownership and security boundaries

Owner: maxfilov4. This repository must remain private. Do not invite contributors or install GitHub applications without the owner's explicit authorization. CODEOWNERS documents ownership; it is not an enforced branch protection rule by itself.

The Android build workflow accepts only runs initiated by maxfilov4 from this same repository. Third-party pull request code must never run with a signing key, server credentials or deployment permissions. Workflow tokens have contents-read permission. APKs contain neither the server's root password nor the APK signing private key.

Current beta signing uses a private Actions cache. This is temporary build infrastructure, not a durable production keystore. Before public distribution, back up the existing signing identity in owner-controlled protected signing infrastructure (do not replace the key used by installed APKs), and configure branch/environment protections from the owner's GitHub settings. The connector used in this session cannot configure repository administration or secrets. No such protection or production key provisioning is claimed here.

The account database does not contain plaintext private-message decryption keys, but encrypted key backups can be attacked offline. The prototype pins contacts' public keys internally, but first use still depends on the directory server (TOFU). The UI hides fingerprint fields as requested by the owner, so this beta has no user-facing out-of-band key verification flow. Private message keys can be restored from a server-side backup encrypted with a backup password or, starting with 0.5.1, an optional device-generated personal recovery key (PBKDF2-SHA256/AES-GCM). Long unique passwords matter; legacy password login also trusts the authentication server with that password, as described below. Existing accounts need to enable their backup once. The server stores end-to-end ciphertext for private/group message history, but sees routing metadata, profiles, membership and ordinary channel MP4 bytes. Channel publications and comments additionally use a signed shared archive so later subscribers can read earlier posts. This archive is encrypted at rest with a server-held key and is not E2E encrypted. The key is in a mode-600 file separate from SQLite and included in installer backups; possession of both allows reading shared channel history and its small attachment keys. Those MP4 uploads are not end-to-end encrypted.

Closed source and APK signatures do not make software unbreakable. APKs can be inspected and modified; Android rejects replacing an already installed app with a differently signed build. This does not stop a person installing a counterfeit app separately. Only distribute verified original builds through owner-controlled channels.

This beta's encryption protocol has not been independently audited and lacks forward secrecy. Before wider release, use an audited ratcheting protocol, review authentication/recovery, perform penetration testing and review the server's operational security. Do not market this beta as suitable for highly sensitive conversations.

The owner supplied a root password in conversation. It is not stored in this repository. Change it through the server console before deployment and use SSH keys for future administration. Keep MFA enabled on GitHub and hosting accounts. Source ownership and release control do not imply a right or technical ability to read users' private messages.

## 0.3 access controls and release integrity

The API checks account tokens and ECDSA envelope signatures, global unique handles, current room membership, channel publishing and pin permissions, owner bans and personal block boundaries. Video uploads are additionally bound to an existing owner nickname plus its public signing-key hash in a root-owned server file. A modified client cannot grant itself this server role. Streaming requires an authenticated current channel member. Files use generated identifiers; upload offsets, total size (2 GiB maximum), chunks and range requests are bounded. Partial uploads never become playable until completed.

HTTPS keeps normal network sniffers from reading credentials. Android enforces hostname verification plus the bundled exact server certificate pin. The server certificate and private key are retained by upgrades; expiry/rotation needs a planned signed client release. A phone owner can inspect the endpoint and, on a compromised/rooted device, extract that device's session or decrypted content; obfuscation cannot provide a guarantee against this. Sessions are per-user, stored only as hashes on the server, expire and can be revoked at logout. APKs contain no universal login or administration key.

Optional updates validate package identity, increasing version, byte size, SHA-256 and the same Android signing certificate before invoking the OS installer. Release archives preserve accounts/TLS configuration and snapshot SQLite before additive migrations. Previous APKs are supported for existing protocol features; new comments and controls require 0.3. The first move from 0.2 is manual because 0.2 did not contain an update checker.

Password login transmits the password only inside pinned TLS. The API server is trusted during login; the password-encrypted backup is not a zero-knowledge recovery protocol and does not protect against an actively malicious authentication server that captures that password.

## 0.5.1 personal recovery and contact discovery

- Private/group message envelopes use AES-256-GCM with a fresh per-message key, RSA-3072 OAEP with SHA-256/MGF1-SHA-256 for key wrapping, and ECDSA P-256/SHA-256 signatures. This is the existing custom protocol, not a newly audited ratchet; it still lacks forward secrecy. Local vaults use Android Keystore-backed AES-GCM. These mechanisms do not protect a conversation against a compromised participant device or copies made by a recipient.
- A personal recovery key is generated on the phone from 32 cryptographically random bytes. It opens only that account's encrypted identity backup. The server receives the encrypted backup, not this key; there is no owner-wide key for subscribers' private messages. Backup encryption uses AES-256-GCM and PBKDF2-HMAC-SHA-256 with 310,000 iterations, a random 16-byte salt and a fresh 12-byte nonce.
- Saving a personal recovery key replaces the active cloud backup and its former backup password. The user must save the key before confirming; losing it prevents restoring that copy on another device. The dialog blocks screenshots and marks manually copied clipboard content as sensitive. A password manager or clipboard provider still receives content the user chooses to copy. Replacing the active backup does not revoke old filesystem backups, captured keys or previously disclosed messages.
- New email-only registrations in 0.5.1 send an independent random server credential instead of the local backup password. Legacy password login still transmits the old credential inside TLS; older registrations already disclosed their then-current credential to the authentication server. This change cannot undo that exposure and does not establish a zero-knowledge or independently audited authentication protocol.
- Contact discovery reads Android contacts only after an explicit action and READ_CONTACTS permission. Names and telephone numbers stay on the phone. The client submits batches of normalized email SHA-256 hashes; the server matches only verified accounts that explicitly enabled discovery, excludes blocked accounts and applies authenticated rate limits. Hashing an email does not make it anonymous: a server can guess likely addresses and hash them. The service does not persist queried address books, but a compromised server could observe queries. This is not private set intersection.
- There is no phone-number account matching because phone numbers are not verified by this beta. The UI explains that matching currently requires the person's verified email to be in the address book. The application requests no WRITE_CONTACTS permission. Test fixtures use emulator-only shell permissions to create and remove their own contacts.
- Custom wallpaper photos are copied into app-private local storage and are not uploaded by the wallpaper feature.

## 0.4 controls and validation

- New account creation requires a hashed, expiring, attempt-limited email challenge. SMTP errors never create an account. The configuration helper verifies TLS and authentication before changing the root-only mail configuration.
- New Android accounts generate separate identities and use distinct encrypted vaults. Account switching clears the active view and media previews. Legacy combined files are preserved outside the active history; unproven selfcopies are not restored. Information already disclosed by an older client cannot be recalled.
- Delete rights are checked by the API: authors and the established moderator for community posts; either recipient or sender for direct messages; creator or established moderator for community deletion. Deletion clears all linked original/derived media and comment records and produces account-scoped tombstones. Files still held by a worker are queued for cleanup and access is revoked immediately. Existing filesystem backups, external copies and older clients may retain prior data.
- Shared channel history verifies a domain-separated ECDSA signature, author identity, channel publish/comment/control permissions and current reader membership. Pagination survives restart; idempotent writes cannot replace prior content; deletion removes archived posts, associated comments/reactions and covers. Legacy posts are exported only by their authenticated author from verified account history. Migration reuses a local attachment only when its owner identity, message ID, channel and content hash match.
- New small media is encrypted before upload. Video circles and channel MP4s remain authenticated server media, not E2E media. The compatible-format worker only processes locally identified media, disables network input protocols, limits concurrency/threads, and cancels after deletion or space exhaustion.
- Calls use the existing authenticated E2E signal envelopes and WebRTC DTLS-SRTP audio. The receiver accepts only unblocked direct-user offers. TURN credentials are time-limited HMAC values minted for an authenticated account; the shared TURN secret stays in root-controlled server configuration. Private/multicast relay destinations are denied, TCP relay allocations disabled, and allocations/bandwidth bounded. Bidirectional audio RTP, authenticated relay use, mute/unmute, incoming/outgoing calls, rejection and background service passed between the Android emulator and an independent aiortc endpoint. Mobile-carrier behavior and physical Xiaomi restrictions have not been measured.
- Release run 34313137338 passed all 38 server tests and nine native Android suites. APK signing identity, manifest/hash agreement, real playback/capture and account isolation were verified. Actual owner SMTP delivery, deployment and physical-device checks remain environment-specific. This is not an independent security audit.
